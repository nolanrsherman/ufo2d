v.07a0 - Where we left off on 4/6/2017

v.07a1 

1. Import laser sprite and set up animation. Add to scene.
2. create a script to handle laser forward momentum.
3. create reusable collision script, implemented on pickup object. 

v.07a2 - CURRENT VERSION
1. PlayerController.cs - 
	a. made movement() method and moved code out of fixedUpdate() and put in its own methdo.
	b. made onClick() method, shoots lazer when mouse is clicked, placed in Update() method.
	
2. DestroyCollision.cs -
	a.change check to other.gameObject.tag instead of .name
	